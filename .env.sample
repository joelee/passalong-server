# Copy to .env and fill in. .env is git-ignored: never commit secrets.
# Empty values are treated as unset.
#
# The server itself needs no secret in its environment: API keys are
# created by `passalong-server key create` and stored hashed, and the TLS
# private key is a file named in config.toml.

# Optional path to config.toml, overriding the default lookup order
# (see docs/configuration.md).
PASSALONG_SERVER_CONFIG_FILE=

# Optional log level override: error | warning | info | verbose | debug
PASSALONG_SERVER_LOG_LEVEL=
