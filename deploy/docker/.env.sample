# Copy to .env. None of these is a secret.
# The port of the host the server is reached at.
PASSALONG_SERVER_PORT=8443
# The tag `docker compose build` gives the image. Nothing pulls or pushes it.
PASSALONG_SERVER_IMAGE=passalong-server:local
